(function(){const root='';const menu='./pcb101应用侧边菜单截图.html';function nav(el){const title=el.dataset.title||'';const back=el.dataset.back||'../index.html';const sub=el.dataset.sub||'';el.className='mobile-nav';el.innerHTML=`<a class="nav-button nav-back" href="${back}" aria-label="返回"></a><h1>${title}${sub?`<span class="sub">${sub}</span>`:''}</h1><a class="nav-button nav-menu" href="${menu}" aria-label="打开快捷菜单"><i></i><i></i><i></i></a>`}document.querySelectorAll('[data-mobile-nav]').forEach(nav);document.querySelectorAll('[data-work-tab]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-work-tab]').forEach(x=>x.classList.remove('active'));button.classList.add('active');const copy=document.querySelector('[data-work-copy]');if(copy){const label=button.textContent.trim();copy.innerHTML=label==='预约'?'您咨询或投递的职位进展，<br>将在这里展示。':`暂无${label}记录，新的进展会及时通知您。`}}));const form=document.querySelector('[data-chat-form]');if(form){const input=form.querySelector('input'),list=document.querySelector('[data-chat-list]');const answer=text=>text.includes('简历')?'已收到，幺弟可以为您做 PCB 岗位关键词分析。':text.includes('东莞')?'好的，正在为您匹配东莞相关岗位。':'幺弟已经记下了，会结合你的技能和意向为你推荐合适机会。';const add=(type,text)=>{const d=document.createElement('div');d.className='message '+type;d.innerHTML=type==='assistant'?`<img class="chat-avatar" src="../assets/yaodi-avatar.png" alt="幺弟"><div class="bubble">${text}</div>`:`<div class="bubble">${text}</div>`;list.appendChild(d);list.scrollTop=list.scrollHeight};form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;add('user',text);input.value='';setTimeout(()=>add('assistant',answer(text)),280)});document.querySelectorAll('[data-chat-shortcut]').forEach(b=>b.addEventListener('click',()=>{input.value=b.dataset.chatShortcut;form.requestSubmit()}));document.querySelector('[data-voice]')?.addEventListener('click',()=>{input.placeholder='语音输入功能已就绪…';setTimeout(()=>input.placeholder='请输入您的问题…',1400)})}})();
